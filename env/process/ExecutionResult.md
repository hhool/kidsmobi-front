我已经将App.tsx中的 Quick Search 按钮移除了。对于 GlobalNews.tsx，目前的组件中已经没有遗留相关Search 的逻辑和模块了，不需要进一步更改。目标任务已完成。

## 进度总结
- 从 `src/App.tsx` 的巨型菜单 (Mega Menu) 中移除了 "Quick Search" 链接
- 检查确认 `src/components/news/GlobalNews.tsx` 中已被移除所有 Search 相关的 UI 和逻辑

## 下一步
请提示下一步的操作指令或者其他需要修改的模块。
